const Note = ( {id, name , message} ) => {
    return (

        <div key={id} >
            <table>
             <tr key=""></tr>

            </table>

        </div>

    );
}

export default Note;